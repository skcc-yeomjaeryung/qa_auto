# 14_9 — 플로우 실사용·런타임 근거 기반 자율 시나리오 증강

- 날짜: 2026-08-06
- Phase: 14.배치테스트
- 회차: 9
- 작업자/Agent: Codex
- 기준 포인터: `docs/index.md` 현재 Phase 14.배치테스트

## 1. 구현 결과

| 영역 | 결과 |
|---|---|
| 그래프 가독성 | 행 전환선을 카드 사이 gutter로 이동하고 조건 라벨을 선 위에 배치 |
| 확대/축소 | 그래프 우측 상단 `-`·`+`·`맞춤`, 60~160% 범위 적용 |
| 단계 I/O | 가짜 GraphQL envelope 제거, DSL 단계의 browser/HTTP operation·실제 입력·최근 실행 관측 출력 |
| 미관측 보호 | 실행 전 단계 output은 `observed:false`로 표시해 성공 관측과 분리 |
| Save/Restore | 시나리오별 scoped runtime 저장·복원, 저장 입력을 재처리 override로 전달 |
| 런타임 탐색 | 등록 계정 로그인 후 GET 화면·DOM snapshot·스크린샷·비파괴 modal opener·BE 계약 수집 |
| 자율 증강 | FE 코드·BE 계약·live DOM·screenshot을 한 생성 입력으로 결합하고 Scenario DSL 근거로 저장 |
| 안전장치 | 업무 form submit 미실행, secret/currentValue 미저장, POST 전용 route 직접 GET 금지 |

## 2. 실데이터 결과

- 그래프: `IG-6d877df71b56`.
- 자율 탐색: `/login`, `/home`, `/signup`, `/`, `/consent`, 등록 계정 로그인 성공.
- 비파괴 UI 관측: `Deposit Funds` → `Make a Deposit`, `Send Payment` → `Send a Payment`.
- 입금 modal DOM: `EXTERNAL ACCOUNT`, `DEPOSIT AMOUNT`, `Deposit`.
- 생성 시나리오: `SCN-deposit-e2e-001-8f98cab1`, `입금 후 완료 안내·잔액·거래내역 정상 반영 확인`, 17단계.
- 생성 근거: `frontend_code`, `backend_contract`, `live_dom`, `screenshot`.
- S9 단계: `wait_for_response`, `POST /deposit`; 미실행 output은 관측 없음으로 유지.

## 3. 검증

- Backend 전체: `177 passed, 6 skipped, 1 warning`.
- Frontend production build: Next.js 15.5.22, 14 route PASS.
- 인앱 브라우저: 줌 120%·맞춤 복귀, 실제 S9 I/O, 입력 저장·복원, 미관측 출력 구분 확인.
- 상세 증적: `artifacts/evidence/phase14-flow-runtime-discovery/VERIFICATION.md`.
- 화면 증적: `artifacts/evidence/phase14-flow-runtime-discovery/flow-step-runtime.png`.

## 4. LLM 상태와 경계

생성·narration LLM 입력에는 런타임 DOM·가시 신호·안전 상호작용·BE 계약을 포함하도록 보강했다. 검증 당시 로컬 LLM endpoint가 연결 거부 상태여서 실제 결과는 근거 기반 결정론 fallback으로 생성됐다. 업무 form은 탐색 중 제출하지 않았으며, 실제 실행 전 output을 성공으로 표시하지 않는다.

## 5. 실행 상태

- Frontend: `localhost:3000` 유지.
- Backend: `127.0.0.1:8000` 유지.
- 사용자가 남은 Phase보다 두 고도화를 우선하도록 지정했으므로 Phase 14 포인터를 유지한다.
