# 14_10 — 금융 시나리오 행렬·증적 다운로드·FLOW 재처리 애니메이션

- 날짜: 2026-08-06
- Phase: 14.배치테스트
- 회차: 10
- 작업자/Agent: Codex
- 기준 포인터: `docs/index.md` 현재 Phase 14.배치테스트

## 1. 원인과 구현 결과

| 영역 | 결과 |
|---|---|
| 기존 금융 1건 | 원본에 있던 `PAYMENT-E2E-001`이 공통 업무로 오분류된 문제 수정 |
| 기존 14건 | 중복 `TRANSACTION-E2E-001` UUID를 동일 graph/case 기준으로 목록에서 제외해 고유 13건 표시(원본 증적 유지) |
| 시나리오 증강 | 실제 form min/max/required/dynamic balance를 정상·경계·예외 행렬로 확장 |
| 최신 생성본 | 고유 23건 · 금융 12건(입금 6, 송금 6) |
| 잔액 입력 | `$165` 고정 금지, 실행 직전 관측 잔액과 step으로 계산 |
| Prompt | 고정 개수 금지, Deposit/Send Payment 분리, 제약별 케이스 유지, 절대 잔액 복사 금지 |
| 실행기 | native constraint 거부 관측, observed balance 기반 입력 전략 지원 |
| 실행 증적 | 이미지 원본 확대·개별 다운로드·전체 raw ZIP 제공 |
| Evidence Package | A→Backend→B 관측 상태, 파일 수, 누락, 무결성·마스킹 시점을 생성 전부터 표시 |
| FLOW | 저장값 재처리 SSE Progress, 선택 단계 강조, 선행환경 복원, 후속 단계별 초록 강조 이동 |

## 2. 실데이터 검증

- 기존 `IG-7fe5b31b7c34`: 금융 거래 2건(입금/송금 각 1건)으로 즉시 보정.
- 최신 `IG-6d877df71b56`: 고유 23건, 금융 거래 12건.
- 입금: 정상·최소 경계·최소 미만·필수 누락·최대 경계·최대 초과.
- 송금: 정상·최소 경계·최소 미만·필수 누락·실행 직전 잔액 전액·잔액 초과.
- 실행 `RUN-ad64f18bcd62`: 패키지 전 A/Backend/B 상태와 raw evidence 15개 표시, ZIP·PNG 다운로드 정상.
- FLOW `S0-login-pw`: 12% 선행환경 복원 중 선택 카드 유지, 50%에서 `S0-login-submit`으로 강조 이동.

## 3. 검증 결과

- Backend 전체: `178 passed, 6 skipped, 1 warning`.
- Frontend production build: Next.js 15.5.22, 14 route PASS.
- Backend/Frontend health: HTTP 200.
- UI 검증은 Codex 인앱 브라우저(`browser:control-in-app-browser`)만 사용.
- 상세 증적: `artifacts/evidence/phase14-scenario-evidence-replay/VERIFICATION.md`.

## 4. LLM 상태와 경계

LLM 입력과 시스템 프롬프트는 보강했다. 다만 검증 당시 로컬 LLM endpoint가 연결 거부 상태였으므로 실제 23건 생성은 근거 기반 결정론 fallback이다. 고정 14건 제한은 없으며, 향후 새 action form·제약·Backend contract가 발견되면 같은 행렬 규칙으로 추가된다.

## 5. 실행 상태

- Frontend: `127.0.0.1:3000` 유지.
- Backend: `127.0.0.1:8000` 유지.
- 사용자가 남은 Phase보다 이번 고도화를 우선하도록 지정했으므로 Phase 14 포인터를 유지한다.
