# 14_8 — 업무 목표 단위 시나리오 생성·실행 결과 집중화

- 날짜: 2026-08-06
- Phase: 14.배치테스트
- 회차: 8
- 작업자/Agent: Codex
- 기준 포인터: `docs/index.md` 현재 Phase 14.배치테스트

## 1. 구현 결과

| 영역 | 결과 |
|---|---|
| Flask/Jinja 분석 | modal opener·title, select, 고유 submit, min/max/step, handler 성공 문구·후속 상태·수치 효과·목록 바인딩을 코드 근거로 추출 |
| 업무 여정 생성 | 컴포넌트별 케이스 대신 로그인 → 업무 진입 → 입력 → API → 성공 문구 → 상태 delta → 신규 목록 행을 한 Scenario DSL로 생성 |
| 표시명 | 코드에서 관측한 결과 상태를 조합해 `입금 후 완료 안내·잔액·거래내역 정상 반영 확인` 생성, LLM fallback과 Console에서 보존 |
| 실행기 | 실제 select, 사전 값/목록 capture, 수치 delta, 신규 목록 행·선택 라벨·입력값 검증, destructive 1회 동의 적용 |
| 판정 | 직접 criterion 관측을 우선해 도달만으로 성공 금지. 성공/실패 원인·근거·조치·재검증·담당자 전달문 생성 |
| 결과 UX | 시나리오에는 기준·입력·간략 결과만 두고, 단계·증적·원인·조치는 실행 이력 상세로 집중 |
| 일시 오류 | 읽기 전용 API에만 네트워크·429·502/503/504 제한 재시도, 성공 재조회 시 과거 오류 문구 제거 |
| 프롬프트 | 작은 모델도 업무 목표, 전후 불변식, 근거 우선, 동일 role 컨트롤을 따르도록 원칙 기반 보강 |

## 2. 실데이터 생성·실행

- 최종 생성: `IG-6d877df71b56`, 14건.
- 최종 입금: `SCN-deposit-e2e-001-67e32d0f`, 17단계.
- 실제 실행: `RUN-0041491ff634`, `amount=30`, 18/18 단계 완료.
- 실제 관측: `Deposit successful`, `$135.00 → $165.00`, `AUG 06 / External Bank / +$30.00`.
- 상태: `WAITING_FOR_REVIEW`; Complete를 HITL Pass로 올리지 않았다.

## 3. 검증

- Backend 전체: `175 passed, 6 skipped, 1 warning`.
- 집중 회귀: `13 passed`.
- Frontend production build: Next.js 15.5.22, 14 route PASS.
- 인앱 브라우저: 최종 제목 3곳, 17단계 업무 여정, 실제 실행 상세, 재진입 3회 오류 0건 검증.
- 상세 증적: `artifacts/evidence/phase14-business-journey-result/VERIFICATION.md`.

## 4. LLM 호출 상태

시나리오 업무 분류·중복 제거·내레이션, 실행 전 DOM 입력 바인딩, 실행 후 관측 요약에서 LLM을 호출할 수 있다. 기대 결과 최종 대조와 세션 선행조건은 현재 결정론 경로다. 로컬 LLM endpoint 연결이 거부되어 이번 검증은 결정론 fallback으로 수행했다.

## 5. 실행 상태

- Frontend: `localhost:3000` LISTEN.
- Backend: `127.0.0.1:8000` LISTEN, `/health` 200.
- 사용자의 화면별 수용 검증이 계속되므로 Phase 14 포인터를 유지한다.
