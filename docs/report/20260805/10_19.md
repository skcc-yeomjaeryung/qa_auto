# 10_19 — 에이전틱 시나리오 생성(B) · 한글 UI · 증적 · 플로우

- 날짜: 2026-08-05
- Phase: 10.Backend추적
- 회차: 19
- 작업자/Agent: Cursor Agent
- 기준 포인터: docs/index.md 현재 Phase 10

## 1. 이번 단계에서 한 일

- D-014 확정: 시나리오 초안 = Graph seed + SDD Plan + LLM narrate/bind (결정론 skeleton 유지, HITL 유지)
- `wf_scenario_dsl` 2스텝화: `generate_dsl` → `scenario_narrate/narrate_and_bind` (LLM 불가 시 결정론 폴백)
- 시나리오 목록/상세 한글화 (PK=`scenarioId`, 서비스 한글 라벨, 단계 서술)
- 분석 중복: 동일 project/role/set/commit/workspace complete 재사용 + 카탈로그 최신 페어만 노출
- Flow: branch key 유일화·중복 분기 제거, 시작→조건→종료 축, spine 재구성
- 실행 증적 API + EvidenceGallery (시나리오/실행 상세)
- agent-browser로 목록·플로우·상세 관측 (증적 PNG 저장)

## 2. 변경·산출물

| 경로 | 목적 |
|---|---|
| `docs/02.아키텍처/03.의사결정기록.md` | D-014 |
| `docs/01.제품과완료기준/02.공통컨텍스트.md` | §7 에이전틱 초안 경로 |
| `backend/app/skills/scenario_narrate/` | narrate Skill |
| `backend/app/core/llm/` | 로컬 LLM 싱글턴 |
| `backend/app/workflow_definitions/wf_scenario_dsl.yml` | 2스텝 Plan |
| `backend/app/services/*analysis*.py`, `console_service.py` | 분석 재사용·카탈로그 |
| `frontend/lib/scenarioLabels.ts`, `ScenarioTable.tsx`, 상세/런 페이지 | 한글 UI·증적 |
| `frontend/components/flow/FlowCanvas.tsx` | key/분기/조건 축 |
| `artifacts/evidence/agent-browser/20260805-*.png` | 관측 스크린샷 |

## 3. 실행·검증

- `bootstrap_runtime`: workflows=9 · skills=10 · capabilities=13
- narrate CLI: `mode=deterministic`, 이름「고객 조회 시나리오」
- compose_graph branch id 유일성 스모크 OK
- `tsc --noEmit` 통과
- agent-browser: `/scenarios` 한글 PK 목록 · `/flow` 조건 pill · 상세 한글 단계 관측
- 실행 runs=0 → 증적 갤러리는 `missing_data` 안내 (실행 후 스크린샷 표시 경로 준비됨)
- Pass/Fail 단정 없음 (관측만)

## 4. 다음 단계

- 시나리오 재생성 시 narrate가 REQUEST/RESPONSE를 채운 초안 확인
- 대상 FE 기동 후 browser_execute → EvidenceGallery 실스크린샷 확인
- 레거시 binding-only 그래프는 재조성 CTA로 교체 유도

## 5. 핵심 근거 (필수)

- 사용자 확정 경로 B + 첨부 plan `scenario_agentic_ux`
- ADR D-014 · 공통컨텍스트 §7
- Hub: `wf_scenario_dsl` + `scenario_narrate`
- 관측 artifact: `artifacts/evidence/agent-browser/20260805-scenarios-list.png`, `...-flow.png`, `...-flow-multi.png`, `...-scenario-detail.png`
