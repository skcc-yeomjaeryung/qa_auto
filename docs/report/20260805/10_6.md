# 10_6 — 로그인 수정 및 프로젝트 온보딩 확인

- 날짜: 20260805
- Phase: 10
- 회차: 6
- 작업자/Agent: Cursor Agent
- 기준 포인터: docs/index.md 현재 Phase 10

## 1. 이번 단계에서 한 일

- 로그인 위자드 단계를 제거하고 즉시 입력 폼 + 「TEST / 1 로 바로 로그인」 버튼을 제공했다.
- ID 대소문자·공백 trim 허용, 로그인 후 `window.location.assign('/')`로 세션 레이스를 피했다.
- AuthProvider가 `/login`에서 「세션 확인 중」에 멈추지 않도록 수정했다.
- Playwright로 TEST/1 로그인 → 프로젝트 생성 → 메뉴 잠금 해제까지 관측했다.

## 2. 변경·산출물

| 경로 | 목적 |
|---|---|
| `frontend/lib/auth.ts` | trim/대소문자 · loginAsDemo |
| `frontend/app/login/page.tsx` | 단순 로그인 UX |
| `frontend/components/AuthProvider.tsx` | login 즉시 렌더 |
| `docs/report/20260805/10_6.md` | 본 회차 |

## 3. 실행·검증

- Playwright: `login-submit` TEST/1 → `/` dashboard
- Playwright: `login-demo` → 프로젝트 `TEST-온보딩-*` 생성 · locked 메뉴 0건
- `npx tsc --noEmit` 통과

## 4. 다음 단계

- 저장소 연결 위자드 → 분석/시나리오 하위 UX 이어서

## 5. 핵심 근거

- 사용자: 「TEST / 1 로그인이 안되는데? 로그인 확인하고 이어서 진행」
- Playwright 관측 artifact (세션 내)
