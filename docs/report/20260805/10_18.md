# 10_18 — 시나리오 다건 생성 · 프로젝트 툴바 · Flow 재처리

- 날짜: 2026-08-05
- Phase: 10
- 회차: 18
- 작업자/Agent: Cursor Agent
- 기준 포인터: docs/index.md 현재 Phase 10.Backend추적

## 1. 이번 단계에서 한 일

- **시나리오 2건 원인**: `customer-search` 하드코딩 템플릿 + TS/JSX-only FE 분석으로 BoA Flask 화면 누락
- Graph 조성·DSL을 다건(엔드포인트/화면/폼 POST) 생성으로 교체
- Flask/Jinja 화면 추출 fallback 추가 (login/deposit/payment/home …)
- 프로젝트 목록 툴바를 chip UI로 정비
- Flow: 카드/디테일 스크롤 · 단계별 재처리 · localStorage 단계 I/O 저장
- SQLite catalog에 scenarios/runs/flow_node_runtime 영속

## 2. 변경·산출물

| 경로 | 목적 |
|---|---|
| `backend/app/skills/scenario_dsl/script/generate_dsl.py` | N시나리오 생성 |
| `backend/app/skills/interaction_graph/script/compose_graph.py` | multi-flow graph |
| `backend/app/skills/frontend_analyze/script/extract_flask_screens.py` | BoA Flask 화면 |
| `frontend/components/flow/FlowCanvas.tsx` | 재처리·localStorage |
| `frontend/components/ProjectsWorkbench.tsx` | 툴바 chip |
| `docs/report/20260805/10_18.md` | 회차 요약 |

## 3. 실행·검증

- 로컬 검증: Flask screens → multi graph → scenarios 다건 (login/deposit/payment 포함)
- Playwright: **시나리오 16건 생성** 관측 (`artifacts/e2e/project-analyze-scenario-2026-08-05T03-46-50-291Z.json`)
- Pass/Fail 단정 없음

## 4. 다음 단계

- FE↔BE 매핑 강화 (form POST ↔ ledger API)
- 파일시스템 artifact(screenshot/video/trace) 경로를 실행 이력 UI에 노출

## 5. 핵심 근거

- 사용자: “2건은 잘못된 결과” · BoA 로그인/입금 등 포함 요구
- 저장 계층 가이드: SQLite / FS / localStorage 분리
- 로컬 스크립트 관측: scenarios=14 (customer-search 단일 템플릿 제거)
