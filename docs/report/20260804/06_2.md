# 06_2 — Console 재기동 · API 수정 · MCP 체인 관측

- 날짜: 2026-08-04
- Phase: 06 (Console 체인 검증 · 수정)
- 회차: 2
- 작업자/Agent: Cursor Agent
- 기준 포인터: docs/index.md (현재 포인터는 07이지만 본 회차는 06 Console 체인 재검증)

## 1. 이번 단계에서 한 일

- Backend `--reload` (8000) · Frontend (3000) 재기동 후 Console API/화면 HTTP 200 확인
- Layout: Journey stepper / repo actions / header button wrap / Flow 빈 canvas fallback 보강
- API/분석 오류 수정: Java `_normalize_java_source`가 `.recordStats()`를 record로 오인 → ledgerwriter 엔드포인트 3건 추출 성공
- Pipeline 성공 시 journey `scenarioCreate`·`scenarioList` 갱신
- agent-browser MCP로 Console 체인 + Bank of Anthos 배포 URL 관측·스크린샷 저장
- sample_java Local Path로 A→API→B Graph(nodes=16, edges=19) FLOW 시각화 확인
- bank-of-anthos GitHub+subdir 파이프라인 재실행 (BE parse 오류 해소 · Graph는 FE Python이라 thin 유지)

## 2. 변경·산출물

| 경로 | 목적 |
|---|---|
| `backend/workers/backend-analyzer/.../spring_parse.py` | record 키워드만 rewrite (recordStats 오인 수정) |
| `backend/app/services/pipeline.py` | pipeline 완료 시 journey 갱신 |
| `backend/app/services/repository_store.py` | journey 필드 확장 업데이트 |
| `frontend/components/flow/FlowCanvas.tsx` | Screen A/B 없을 때 empty + bindings 표시 |
| `frontend/app/styles.css` | flow-empty · header-actions wrap |
| `artifacts/evidence/phase06-console/*.png` | MCP 관측 스크린샷 |

## 3. 실행·검증

- 기동: `uvicorn ... --reload :8000`, `next dev :3000`
- Console API: `/health`·`/api/projects`·pipeline 200
- sample_java pipeline: complete · `IG-106aa76e28a2` · nodes=16 edges=19 · scenario READY_FOR_INPUT
- BoA pipeline: complete · BE endpoints GET `/version`, `/ready`, POST `/transactions` · parse unresolved 0
- 배포 URL: `https://cymbal-bank.fsi.cymbal.dev` — Sign In → Checking Account / Transaction History 화면 관측
- pytest: `test_sdd_phase00b` + `test_backend_analysis_phase03` → 10 passed
- Pass/Fail·배포 확정 없음 (관측만)

### 스크린샷 목록

1. `01-projects-layout.png` ~ `04-sync-complete.png` — Projects · Sync
2. `05-scenarios-list.png` · `10-scenarios-final.png` — Scenarios
3. `06-flow-canvas.png` — thin graph (수정 전 빈 lane 관측)
4. `07-flow-sample-java.png` — A→API→B FLOW 렌더
5. `08-boa-deploy-login.png` · `09-boa-home-after-login.png` — 배포 URL

## 4. 다음 단계

- Phase 07 컴포넌트계약 진행 전, HITL/테스트실행 스텝은 별도 승인
- BoA FE(Python) 정적분석 범위는 React/TS analyzer와 불일치 → missing_data/thin graph는 정상 관측
- in-memory store는 reload 시 초기화됨 — 재검증 시 프로젝트 재등록 필요

## 5. 핵심 근거 (필수)

- 사용자 지시: 재기동 · layout/API 수정 · MCP 적극 활용 · 완료 스크린샷
- 배포 URL: [Bank of Anthos](https://github.com/GoogleCloudPlatform/bank-of-anthos.git) 공개 데모 `https://cymbal-bank.fsi.cymbal.dev`
- BE parse 버그: `.recordStats()` → `class Stats` 오치환 (workspace `LedgerWriterController.java`)
- Evidence: `artifacts/evidence/phase06-console/`
- Gate Pass/Fail 단정 없음
