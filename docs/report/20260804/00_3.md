# 00_3 — 시각 컨셉 확인 (A→API→B + Console)

- 날짜: 2026-08-04
- Phase: 00.기반구축
- 회차: 3
- 작업자/Agent: Cursor Agent
- 기준 포인터: `docs/index.md` → **00.기반구축** (Gate 일부 제약으로 유지)

## 1. 이번 단계에서 한 일

- Control Plane · Web Console · Sample FE · Sample BE를 기동했다.
- Sample FE에서 `CUS-1001` 입력 후 Search를 눌러 Backend 호출 → B 화면 이동을 화면 DOM·스크린샷으로 관측했다.
- CORS가 `localhost`만 허용해 `127.0.0.1` Origin에서 실패하던 문제를 수정했다.
- Web Console에서 Control Plane Online · 여정 스텝퍼 · Visual demo 버튼을 관측했다.
- `make up-dev`의 macOS/`setsid` 부재·IDE 셸 종료 이슈를 `nohup` 경로로 완화했다 (Cursor 세션에서는 백그라운드 셸 기동 권장).

## 2. 변경·산출물

| 경로 | 목적 |
|---|---|
| `sample-targets/customer-service-be/.../WebConfig.java` | `127.0.0.1` CORS 허용 |
| `sample-targets/customer-portal-fe/src/App.tsx` | API 기본 URL `127.0.0.1:8080` |
| `scripts/dev-up.sh` / `dev-down.sh` | macOS 친화 기동·종료 |
| `artifacts/evidence/phase00-smoke/*.png` | 입력·상세·Console 스크린샷 |
| `docs/06.완료보고/PHASE-00.md` | Gate 관측 갱신 |

## 3. 실행·검증

관측(품질 Pass/Fail·HITL 승인 단정 아님):

| 대상 | 관측 |
|---|---|
| A 화면 입력 | `CUS-1001` 입력 상태 스크린샷 |
| B 화면 | `Kim Pilot` / `LOW` / `ACTIVE` 표시 |
| Console | Control Plane Online · Journey 스텝 |
| API | `POST /api/customers/search` → 200 JSON |

Evidence:
- `artifacts/evidence/phase00-smoke/01-search-input.png`
- `artifacts/evidence/phase00-smoke/02-customer-detail.png`
- `artifacts/evidence/phase00-smoke/03-web-console.png`

## 4. 다음 단계

- Docker 미설치로 PostgreSQL/Redis Compose 실기동은 미검증 → 환경 준비 후 `make infra-up`
- 사람(PL/QA)이 Gate·시각 컨셉을 확인한 뒤 Phase 01 포인터 갱신 여부 결정
- 로컬에서 버튼으로 재확인:
  - Console: http://127.0.0.1:3000/
  - Sample FE: http://127.0.0.1:5173/customers/search (`CUS-1001`)

## 5. 핵심 근거

- 사용자 지시: 시스템 프롬프트 이후 **실제 구현 + 버튼 눌러 시각 컨셉 확인**
- SSOT Phase: `docs/index.md` / `docs/04.Phase실행바이블/00.기반구축.md`
- 보고 규칙: `.cursor/rules/03-post-report.mdc` → `docs/report/YYYYMMDD/{phase}_{n}.md`
- 브라우저 경로: 사용자 “버튼 눌러 확인” 요청에 따라 agent-browser로 DOM·스크린샷 관측 (파괴적 크롤 없음)
- CORS 실패 Evidence: Origin `http://127.0.0.1:5173` → `Invalid CORS request` → WebConfig 수정 후 200
- Gate 미완 항목: Docker 부재로 Postgres/Redis 일괄 기동 Evidence = `missing_data` → 포인터 00 유지
