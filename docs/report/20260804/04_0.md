# 04_0 — 모노레포 frontend/backend 이관 (D-011)

- 날짜: 2026-08-04
- Phase: 04.API매핑 (선행 정리)
- 회차: 0 (구조 정리)
- 작업자/Agent: Cursor Agent

## 1. 이번 단계에서 한 일

- 사용자 요청으로 루트를 `frontend/` · `backend/` 로 재배치했다.
- 구 `apps/` 삭제 사고로 소스 유실 → agent transcript Write 스냅샷으로 복구 후 `frontend/`·`backend/`에 안착.
- 루트 `workers/` → `backend/workers/`.
- SSOT·Makefile·dev-up을 D-011에 맞게 갱신.
- dead 구조 `apps/` 제거 유지.

## 2. 변경·산출물

| 경로 | 목적 |
|---|---|
| `frontend/` | Web Console |
| `backend/` | Control Plane + workers |
| `docs/02.아키텍처/*` | D-011 · 모노레포 |
| `Makefile` · `scripts/dev-up.sh` | 경로 갱신 |

## 3. 실행·검증

| 항목 | 결과 |
|---|---|
| `backend` pytest | 8 passed |
| `backend/workers/frontend-analyzer` npm test | 11 passed |
| `backend/workers/backend-analyzer` pytest | 1 passed |
| `frontend` next build | OK |
| `scripts/dev-up.sh` | READY :8000 :3000 :5173 :8080 |

복구 보완: `repository_store` analysis/token API, KoPub 폰트, `pyproject` workers exclude, `_redact` Python 3.12 호환.

## 4. 다음 단계

- Phase 04 API 매핑 (대상 FE/BE 분석 결과 join)

## 5. 핵심 근거

- 사용자: “qa_auto 루트에 frontend/backend 구성… 잘못된 디렉토리 이관·dead code 삭제”
- D-011
