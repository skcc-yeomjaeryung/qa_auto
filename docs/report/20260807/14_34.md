# 14_34 — HITL 실행 리포트 시각화·전체 화면 증적 보강

- 날짜: 2026-08-07
- Phase: 14.배치테스트
- 회차: 34
- 작업자/Agent: Codex
- 기준 포인터: `docs/index.md` 현재 Phase 14

## 1. 이번 단계에서 한 일

- 사용자가 제공한 구형 다운로드 리포트와 `RUN-576cd6cf4076` 원본을 대조했다.
- 증적 패키지는 16개 파일이지만 대표 화면은 3장이고, 실행 원본에는 단계별 화면 12장이 존재함을 확인했다.
- 기존 HTML이 13개 JSON·로그를 `pre`로 펼치면서 인쇄/PDF 본문을 점유하고, 화면 이미지는 대표 3장만 포함한 원인을 수정했다.
- 리포트 본문을 실행 진행률·관측 분포·A 화면→Backend→B 화면·기술 검증 수치·대표 화면·전체 실행 캡처 중심으로 재구성했다.
- Frontend 대시보드의 QA 로봇을 리포트 도우미 캐릭터로 포함했다.
- 원시 JSON·스냅샷·Network·로그는 본문에서 제거하고 16건 파일 인벤토리와 JSON/증적 ZIP으로 분리했다.
- 최종 Pass/Fail은 생성하지 않고 `PENDING_HUMAN_REVIEW` 계약을 유지했다.

## 2. 변경·산출물

| 경로 | 목적 |
|---|---|
| `backend/app/skills/run_report/script/generate_report.py` | 인쇄 가능한 시각 우선 HTML, 대표 3장·단계별 12장, Progress·여정·관측 분포·인벤토리 생성 |
| `backend/app/skills/run_report/SKILL.md` | 1.1.0 시각/인쇄 계약, 원시 로그 본문 금지, Figma Progress Kit 근거 추가 |
| `backend/app/workflow_definitions/wf_run_report.yml` | Workflow 1.1.0 시각 우선 출력 계약 |
| `backend/tests/test_run_report_agent.py` | 전체 인벤토리·화면 캡처·Progress·원시 로그 0 회귀 테스트 |
| `artifacts/reports/runs/RUN-576cd6cf4076/report.html` | 실제 Run 강제 재생성 리포트 |
| `/Users/a11123/Downloads/RUN-576cd6cf4076-review-report (5).html` | HITL 화면의 정상 다운로드 버튼으로 받은 사용자 확인용 산출물 |

## 3. 실행·검증

- Figma Progress Bar UI Kit `HLWN6f7fxSVMIxoZtW6SIc`를 MCP로 조회했다.
  - Components canvas `799:98620`
  - Type 1 Complete `266:199`
  - Type 1 Progressing `266:196`
  - Type 5 Progressing `295:3878`
- Report Agent 집중 테스트: `4 passed`.
- Backend 전체 회귀: `234 passed, 6 skipped`, 의존성 deprecation warning 1건.
- 실제 Run 강제 재생성 API: HTTP 200.
- 생성 HTML 관측:
  - 전체 이미지 16개 = QA 캐릭터 1 + 대표 화면 3 + 단계별 실행 캡처 12
  - `pre` 원시 로그 0개
  - 증적 파일 인벤토리 16/16
  - 진행 막대 1개
  - 스크롤 의존 CSS 0개
- HITL 화면의 `리포트 다운로드` 버튼으로 새 파일이 실제 Downloads에 생성됨을 확인했다.
- 서버 산출물과 다운로드 파일 SHA-256이 `45946548fd67283b945272ca118887c1d9d698d2a6154a8d3ef26d4517fef233`으로 동일했다.
- 인앱 브라우저에서 HITL drawer의 Run·증적 16건·생성 시각·다운로드 CTA를 화면 DOM·스크린샷 기준으로 관측했다.

## 4. 다음 단계

- 사용자가 새 `(5).html`을 실제 인쇄/PDF 미리보기에서 확인하고, 썸네일 크기·페이지 분할에 대한 선호가 있으면 인쇄 전용 CSS만 미세 조정한다.
- 리포트 JSON과 증적 ZIP은 원본 감사 자료로 유지하고 HTML 본문에 원시 로그를 다시 펼치지 않는다.
- Phase 포인터는 사용자 화면별 수용 검증이 계속되는 동안 14로 유지한다.

## 5. 핵심 근거

- 사용자 제공 구형 리포트: `/Users/a11123/Downloads/RUN-576cd6cf4076-review-report (4).html`
- 실제 Run 원본: `artifacts/evidence/runs/RUN-576cd6cf4076/`
- Evidence Package: `artifacts/evidence/packages/EVID-RUN-576cd6cf4076/manifest.json`
- 재생성 리포트: `artifacts/reports/runs/RUN-576cd6cf4076/report.html`
- Figma Progress UI SSOT: `docs/02.아키텍처/04.진행상태UI.md`
- 최종 승인 상태는 담당자 범위이며 이번 검증은 기술 산출물 관측이다.
