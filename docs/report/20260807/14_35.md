# 14_35 — HITL 증적 건수·선행조건 진단·리포트 보기 보정

- 날짜: 2026-08-07
- Phase: 14.배치테스트
- 회차: 35
- 작업자/Agent: Codex
- 기준 포인터: `docs/index.md` 현재 Phase 14

## 1. 이번 단계에서 한 일

- 사용자 다운로드 리포트 `(6).html`, Evidence Package manifest, 실행 단계 캡처를 다시 대조했다.
- `16건`은 패키지에 보존된 전체 파일 수이고, 화면 증적은 대표 3장 + 실행 단계 캡처 12장 = 15장임을 확인했다. 16번째 화면 이미지가 누락된 상태가 아니다.
- 실제 파일은 16/16 존재하는데 실행의 의미상 미관측 항목을 파일 손상으로 합산해 `partial`로 보이던 무결성 분류를 분리했다.
- 제출 단계가 중단되면 성공 문구·목록 변화·값 변화가 함께 미관측되는 세 항목을 독립 장애처럼 세지 않고 하나의 연쇄 영향으로 묶었다.
- 실행 입력을 Run의 실제 입력값으로 Evidence Package에 연결하고, 외부 테스트 대상은 내부 서버 추적 부재를 패키지 손상으로 판단하지 않도록 보정했다.
- 증적 썸네일 클릭 시 새 `data:image/...` 페이지로 이동하지 않고 리포트 안의 확대 대화상자로 열리도록 수정했다.
- 자동화 반복 실패를 실제 샌드박스에서 재현해 계좌 잔액 0.00으로 인해 송금 입력의 `min=0.01`, `max=0.00` 조건을 동시에 만족할 수 없었던 선행조건 부족으로 확정했다.
- 제출 직전에 브라우저의 실제 폼 유효성을 검사하고, 불가능한 입력이면 POST 전에 한 가지 원인과 해결 방법을 안내하도록 보강했다.
- 숫자 변화 비교가 `0.00 → 0.00`을 기대값 `-0.01`과 오차 범위 내 일치로 잘못 보던 로직을 센트 단위 정확 비교로 수정했다.

## 2. 변경·산출물

| 경로 | 목적 |
|---|---|
| `backend/app/services/evidence_package.py` | Run 입력 프로필 연결, 실제 파일 기준 Evidence Package 무결성 판정 |
| `backend/app/services/telemetry/service.py` | 외부 대상의 내부 서버 추적 제약을 파일 수집 실패와 분리 |
| `backend/app/skills/run_report/script/generate_report.py` | 파일/화면 건수 분리, 연쇄 증상 축약, 리포트 내부 이미지 확대 대화상자 |
| `backend/app/skills/browser_execute/script/execute_run.py` | 제출 전 native form 선행조건 검사, 단일 원인 관측, 센트 단위 변화 검증 |
| `backend/app/services/run_service.py` | 테스트 데이터 선행조건 부족의 사용자용 원인·담당·조치·재검증 안내 |
| `frontend/lib/interactiveRun.ts` | 실행 API에 실제 입력값 전달 계약 추가 |
| `frontend/components/ScenarioRunConsole.tsx` | 시나리오 기본 입력과 사용자의 수정 입력을 Run에 함께 저장 |
| `frontend/lib/evidenceLabels.ts` | `input_precondition_invalid` 사용자용 라벨·안내 |
| `frontend/components/RunEvidenceDrawer.tsx` | 선행조건 부족을 별도 이슈 유형으로 표시 |
| `artifacts/reports/runs/RUN-576cd6cf4076/report.html` | 기존 실패 Run의 건수·무결성·확대 보기 보정 리포트 |
| `artifacts/reports/runs/RUN-8a3a0951a0df/report.html` | 정상 선행조건에서 재실행한 송금 기술 관측 리포트 |

## 3. 원인과 건수 판정

### 3.1 Evidence Package 16건과 화면 15장

| 구분 | 건수 | 설명 |
|---|---:|---|
| 패키지 파일 | 16 | 시나리오·입력·Commit·요청/응답·Network·snapshot·화면 이미지 등 전체 감사 파일 |
| 대표 화면 | 3 | A 입력, Backend 요청·응답, B 결과의 대표 이미지 |
| 실행 단계 화면 | 12 | 브라우저 실행 중 단계별로 저장된 전체 캡처 |
| 시각 증적 합계 | 15 | 대표 3 + 실행 단계 12 |

따라서 리포트의 `15장`은 16개 패키지 파일 가운데 화면으로 볼 수 있는 자료와 Run 단계 캡처를 합친 수치다. 패키지 파일 16/16은 모두 존재하며 파일 무결성은 `complete`다.

### 3.2 기존 Run의 미관측 자료

- `RUN-576cd6cf4076`은 제출 전 안전 정책 중단으로 Backend 요청·응답·이벤트와 성공 결과 관측이 실제로 발생하지 않은 과거 Run이다.
- 파일이 사라진 것이 아니라 실행이 제출 전에 끝났기 때문에 만들 수 없었던 의미상 자료다.
- 성공 문구·목록 변화·값 변화 3건은 같은 제출 중단에서 파생된 연쇄 결과로 묶어 표시한다.
- 과거 Run의 사실을 성공으로 바꾸지 않고, 파일 무결성과 실행 미관측 사유를 분리해 사용자에게 보여준다.

### 3.3 반복 실패의 근본 원인

- 연결된 화면의 송금 입력 계약은 `min=0.01`, `max=현재 잔액`이다.
- 재현 시점 계좌 잔액이 `0.00`이어서 양수 송금액은 어떤 값도 유효하지 않았다.
- 수동으로 수행한 195.00 송금이 계좌 잔액을 0.00으로 만든 뒤 동일 계정을 재사용한 것이 반복 실패의 직접 원인이다.
- 자동화 화면의 두 이슈는 별개 장애가 아니라 유효한 금액 제출이 불가능해 성공 문구와 목록/값 변화가 모두 생기지 않은 연쇄 현상이다.

## 4. 실행·검증

### 4.1 실제 샌드박스 재현

| Run | 입력/목적 | 기술 관측 |
|---|---|---|
| `RUN-b0046bf44bb5` | 잔액 0.00에서 송금 0.01 | POST 전 중단, `input_precondition_invalid` 한 가지 원인으로 안내 |
| `RUN-56a07f943521` | 테스트 데이터 복구를 위한 입금 1.00 | `Deposit successful`, 잔액 0.00→1.00, 새 거래, HTTP 200 |
| `RUN-8a3a0951a0df` | 송금 0.01 재실행 | `Payment successful`, 잔액 1.00→0.99, 새 거래, HTTP 200, missingData 0 |

- 위 검증은 사용자가 허용한 샌드박스 범위에서 입금 1.00과 송금 0.01을 실제로 생성했으며, 검증 직후 잔액은 0.99다.
- 이는 자동 기술 관측 결과이며 최종 Pass/Fail과 HITL 승인은 담당자가 확정한다.

### 4.2 리포트·브라우저

- `RUN-576cd6cf4076` Evidence Package: artifact 16/16, `integrityStatus=complete`.
- 재생성 리포트: 패키지 파일 16, 시각 증적 15, 대표 3 + 실행 단계 12를 각각 명시.
- 대표/단계 화면 버튼 클릭 시 같은 문서의 확대 대화상자가 열리고, `data:image/png;base64` 이미지가 정상 표시됨을 확인했다.
- 보고서 페이지와 앱 콘솔 오류가 없음을 확인했다.
- HITL drawer에서 `RUN-8a3a0951a0df`, 증적 16건, 외부 대상 관측, 리포트 다운로드를 확인했다.

### 4.3 회귀

- Backend 집중 테스트: `57 passed, 1 skipped`.
- Backend 전체 회귀: `238 passed, 6 skipped`, 의존성 deprecation warning 1건.
- Frontend production build: PASS, TypeScript 검사 포함.

## 5. 다음 단계

- 테스트 계정은 시나리오 시작 전에 필요한 잔액·데이터 상태를 fixture로 복원하고, 배치 실행에서도 동일 계정을 소진한 채 재사용하지 않는다.
- 파일 무결성, 실행 제약, 업무 관측 누락은 계속 별도 상태로 유지한다.
- 기존 실패 Run은 과거 실행 사실을 보존하고, 정상 선행조건으로 생성한 새 Run에서 기술 결과를 비교한다.
- Phase 포인터는 사용자 화면별 수용 검증이 계속되는 동안 14로 유지한다.

## 6. 핵심 근거

- 사용자 다운로드 리포트: `/Users/a11123/Downloads/RUN-576cd6cf4076-review-report (6).html`
- 사용자 제공 Data URI: `/Users/a11123/.codex/attachments/46077cad-b098-4e9e-89c6-9359cb72d2ae/pasted-text.txt`
- 기존 Run 원본: `artifacts/evidence/runs/RUN-576cd6cf4076/`
- Evidence Package: `artifacts/evidence/packages/EVID-RUN-576cd6cf4076/manifest.json`
- 보정 리포트: `artifacts/reports/runs/RUN-576cd6cf4076/report.html`
- 정상 선행조건 재실행 리포트: `artifacts/reports/runs/RUN-8a3a0951a0df/report.html`
- 최종 승인 상태는 담당자 범위이며 이번 검증은 기술 산출물 관측이다.
