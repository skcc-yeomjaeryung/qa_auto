# Contracts

Shared JSON Schema package. `npm run sync-schemas` copies the SSOT schemas from
`docs/03.계약과예시/schemas/` into `schemas/`; the two Phase 00 platform
schemas live here directly. Run `npm test` to synchronize and parse-check all schemas.
